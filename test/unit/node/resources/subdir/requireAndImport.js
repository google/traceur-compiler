import {x} from '../reexport-x';
var path = require('path');
var aNodeModule = require('../aNodeModule.js');
export var testForRequireAndImport =
    `we have path and x=${x} and aNodeExport=${aNodeModule.aNodeExport}`;
